# yaml-language-server: $schema=https://raw.githubusercontent.com/thankjura/luna-schemas/refs/heads/master/luna-plugin.schema.json?v3

key: ${project.groupId}.${project.artifactId}
name: ${project.name}
version: ${project.version}
description: ${project.description}
vendor:
  name: ${project.organization.name}
  url: ${project.organization.url}
resources:
  - key: messages
    path: messages
    type: i18n
components:
  - key: HelloWorldRest
    name: Test rest controller
    className: ru.slie.luna.demo.HelloWorldRest